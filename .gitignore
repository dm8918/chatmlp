# Python
__pycache__/
*.py[cod]
.pythonlibs/
.upm/
.cache/

# Node
node_modules/
# NOTE: frontend/dist is intentionally committed so the built React app ships
# with the Databricks App deploy (Databricks Apps do not build the front-end).

# Env
.env
